<html>
<head>
    <title>Places</title>
</head>
<body>
    <h1>Places I've Been</h1>

    {% if places is not empty %}
        <p>Here are all your places:</p>
        <ul>
            {% for place in places %}
                <li>{{ place.getName }}</li>
            {% endfor %}
        </ul>
    {% endif %}

    <form action='/places' method='post'>
        <label for='placeName'>Where Is This Place?</label>
        <input id='placeName' name='placeName' type='text'>
        <button type='submit'>Add Place</button>
    </form>
    <form action='/clear_places' method='post'>
        <button type='submit'>Clear</button>
    </form>
</body>
</html>
